void main() {
  // models -> representação de um objeto
  // repositories -> representa a busca e dados e conversão para os modelos
  // services -> regra de negócios (fluxos)
  // controller -> entrada dos dados
}